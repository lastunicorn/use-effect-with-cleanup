# React `useEffect` Demo (Timer)

Demo application that shows how to use the `useEffect` hook in React.

There are multiple demo repositories for `useEffect`. This one is creating a timer and starts/stops/resets it.

Uses Vite as build tool.

See the [Tutorial](/doc/tutorial/README.md) for more details.

